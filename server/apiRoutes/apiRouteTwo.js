const express = require('express');
const router = express.Router();

// matches GET requests to /api/apiRouteTwo/
// router.get('/', function (req, res, next) {
  // /* etc */ });
// matches POST requests to /api/apiRouteTwo/
// router.post('/', function (req, res, next) {
  // /* etc */ });
// matches PUT requests to /api/apiRouteTwo/:apiRouteTwoId
// router.put('/:apiRouteTwoId', function (req, res, next) {
//   /* etc */ });
// matches DELETE requests to /api/apiRouteTwo/:apiRouteTwoId
// router.delete('/:apiRouteTwoId', function (req, res, next) {
//   /* etc */ });

module.exports = router;
